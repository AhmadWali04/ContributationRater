"""
file: src/commands/project/cm_report.py
function:
    Responsible for generating a report of user activity for a given timespan and project.
commands:
    - /report [project]
    - /report [user] [project]
    - /report [user]
    - /report [project] [timespan]
    - /report [user] [project] [timespan]
"""

import os
import sys
import asyncio

from discord.ext import commands, tasks

# TODO: Implement cm_report.py commands/tasks.
