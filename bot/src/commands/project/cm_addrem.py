"""
file: src/commands/project/cm_addrem.py
function:
    Responsible for handling adding and removing projects and users from contribots config.
commands:
    -/add [user] [project]
    -/add [project] [config]
    -/remove [user] [project]
    -/remove [project]
"""

import os
import sys
import asyncio

from discord.ext import commands, tasks

# TODO: Implement cm_addrem.py commands/tasks.
